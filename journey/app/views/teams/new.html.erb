<header class="topbar">
  <div class="brand-pill"><%= t("teams.new", default: "New Team") %></div>
  <div class="topbar__spacer"></div>
</header>

<main class="page">
  <section class="page__container">
    <%= form_with model: @team do |f| %>
      <div class="field">
        <label class="label"><%= t("teams.fields.name", default: "Team name") %></label>
        <%= f.text_field :name, class: "input", placeholder: "Product, Marketing, …" %>
      </div>
      <%= f.submit t("common.create", default: "Create"), class: "btn btn-primary" %>
    <% end %>
  </section>
</main>
